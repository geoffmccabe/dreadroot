# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.next/

# Secrets / environment — NEVER commit (service_role key etc.)
.env
.env.*
!.env.example

# perftest auth/session state — never commit
.perftest/
perftest-results/
3d_assets/

# Oversized assets must not ship to Cloudflare Pages (25MB/file limit)
public/PlayerSkins.fbx

# Dev-source assets — never serve these; the game loads converted .glb/.gltf/.webp only.
# (They blew past GitHub's 100MB limit and bloated the repo.)
public/Starblind 3D Character Models/
public/Synty Animation Packs/
*.blend
*.blend1
*.unitypackage
public/*.psd
.city-cache/
