# Block all lifecycle scripts by default
ignore-scripts=true

# ktx2-encoder transitively conflicts with @react-three/fiber's peer dep range.
# Required so Cloudflare Pages' npm install step doesn't ERESOLVE-fail the build.
legacy-peer-deps=true
