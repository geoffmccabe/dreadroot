// Shared collider-overlay stats, published by ColliderDebugView and shown inside
// the existing bottom-left collider readout (VoxelizeTool) — so there's ONE
// readout, not a second modal.
export const colliderDebugStats = { on: false, green: 0, blue: 0, tris: 0 };
