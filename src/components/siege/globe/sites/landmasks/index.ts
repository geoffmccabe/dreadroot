// Every generated land mask, imported for its side effect of registering itself.
//
// ADD ONE LINE PER COASTAL CITY. An inland city needs no entry: landFractionFor returns null and the
// ground override treats the whole footprint as land, which is correct there.
import './dubai';
